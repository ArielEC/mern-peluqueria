{
  "name": "mern-peluqueria",
  "version": "1.0.0",
  "description": "Sistema de reservas MERN — scripts de arranque",
  "private": true,
  "scripts": {
    "start":      "concurrently -n BACK,FRONT -c cyan,magenta \"npm start --prefix backend\" \"npm run preview --prefix frontend\"",
    "dev":        "concurrently -n BACK,FRONT -c cyan,magenta \"npm run dev --prefix backend\"   \"npm run dev --prefix frontend\"",
    "build":      "npm run build --prefix frontend",
    "seed":       "node backend/src/scripts/seedAdmin.js",
    "migrate:blockers-created-by": "npm run migrate:blockers-created-by --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
